@using LaboratoryWork3.ViewModels
@inject RedirectToLoginViewModel RedirectToLoginVm

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {

        if (firstRender)
        {
            await RedirectToLoginVm.RedirectToLoginIfNeededAsync();
        }
    }
}